---

# This isn't a thing anymore with Xenial and Stretch.
#default_docker_dependencies:
#  - linux-image-extra-virtual
default_docker_dependencies: []

python_pip_packages:
  - python-pip

python_sni_support_packages:
  - python-dev
  - libssl-dev
  - libffi-dev
